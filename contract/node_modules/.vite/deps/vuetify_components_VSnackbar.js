import "./chunk-3DEACMDC.js";
import {
  VuetifyLayoutKey,
  useLayout
} from "./chunk-EITUF6OD.js";
import {
  VProgressCircular
} from "./chunk-D5C6FLOU.js";
import "./chunk-LQ4ACLQT.js";
import {
  VOverlay,
  makeVOverlayProps
} from "./chunk-YEE5O2WR.js";
import {
  useScopeId
} from "./chunk-IXJATSQG.js";
import "./chunk-Q7BKIUQY.js";
import {
  forwardRefs
} from "./chunk-5ZIYHH3Y.js";
import {
  VAvatar
} from "./chunk-OO3JGPNN.js";
import "./chunk-3CNVK67G.js";
import "./chunk-ZUO5DEWM.js";
import {
  makePositionProps,
  usePosition
} from "./chunk-U7SXKSBZ.js";
import {
  VProgressLinear
} from "./chunk-P3OU324N.js";
import {
  genOverlays,
  makeVariantProps,
  useVariant
} from "./chunk-Y4XNKRRE.js";
import {
  makeLocationProps,
  makeRoundedProps,
  useRounded
} from "./chunk-5WIJD3CK.js";
import {
  VIcon
} from "./chunk-3SFGMP2B.js";
import {
  IconValue
} from "./chunk-KNSB6IH6.js";
import "./chunk-LGRGM6PL.js";
import {
  VDefaultsProvider
} from "./chunk-KNWTDDGP.js";
import {
  useResizeObserver
} from "./chunk-V5N7IPZH.js";
import "./chunk-BZLMOOCU.js";
import {
  makeThemeProps,
  provideTheme
} from "./chunk-DOBPEDAR.js";
import "./chunk-PGYPCZ4I.js";
import "./chunk-B2C354F2.js";
import {
  useProxiedModel,
  useToggleScope
} from "./chunk-YJ5EV5N3.js";
import "./chunk-6H2U4HSN.js";
import "./chunk-QIOIWFAY.js";
import {
  convertToUnit,
  genericComponent,
  omit,
  propsFactory,
  refElement,
  useRender
} from "./chunk-6TYRTSVH.js";
import {
  Fragment,
  computed,
  createBaseVNode,
  createVNode,
  inject,
  mergeProps,
  nextTick,
  normalizeClass,
  onBeforeUnmount,
  onMounted,
  onScopeDispose,
  ref,
  shallowRef,
  useId,
  watch,
  watchEffect
} from "./chunk-RPENW4GS.js";
import "./chunk-HKJ2B2AA.js";

// .yarn/__virtual__/vuetify-virtual-cc83312558/3/.yarn/berry/cache/vuetify-npm-4.0.6-051f960ad8-10c0.zip/node_modules/vuetify/lib/components/VSnackbar/VSnackbar.js
import "/home/chen/work-space/contract/.yarn/__virtual__/vuetify-virtual-cc83312558/3/.yarn/berry/cache/vuetify-npm-4.0.6-051f960ad8-10c0.zip/node_modules/vuetify/lib/components/VSnackbar/VSnackbar.css";

// .yarn/__virtual__/vuetify-virtual-cc83312558/3/.yarn/berry/cache/vuetify-npm-4.0.6-051f960ad8-10c0.zip/node_modules/vuetify/lib/components/VSnackbarQueue/queue.js
var VSnackbarQueueSymbol = Symbol.for("vuetify:v-snackbar-queue");
function useSnackbarItem(isActive, contentEl) {
  const queue = inject(VSnackbarQueueSymbol, null);
  if (!queue) return null;
  const id = useId();
  queue.register(id);
  onBeforeUnmount(() => queue.unregister(id));
  watch(isActive, (val) => !val && queue.unregister(id), {
    flush: "sync"
  });
  const {
    resizeRef,
    contentRect
  } = useResizeObserver();
  watch(contentEl, (el) => {
    resizeRef.value = el ?? null;
  });
  watch(contentRect, (rect) => {
    if (rect?.width) queue.setSize(id, rect.height, rect.width);
  });
  const offset = computed(() => queue.getOffset(id));
  return {
    id,
    offset
  };
}

// .yarn/__virtual__/vuetify-virtual-cc83312558/3/.yarn/berry/cache/vuetify-npm-4.0.6-051f960ad8-10c0.zip/node_modules/vuetify/lib/components/VSnackbar/VSnackbar.js
function useCountdown(milliseconds) {
  const time = shallowRef(milliseconds());
  let timer = -1;
  function clear() {
    clearInterval(timer);
  }
  function reset() {
    clear();
    nextTick(() => time.value = milliseconds());
  }
  function start(el) {
    const style = el ? getComputedStyle(el) : {
      transitionDuration: 0.2
    };
    const interval = parseFloat(style.transitionDuration) * 1e3 || 200;
    clear();
    if (time.value <= 0) return;
    const startTime = performance.now();
    timer = window.setInterval(() => {
      const elapsed = performance.now() - startTime + interval;
      time.value = Math.max(milliseconds() - elapsed, 0);
      if (time.value <= 0) clear();
    }, interval);
  }
  onScopeDispose(clear);
  return {
    clear,
    time,
    start,
    reset
  };
}
var makeVSnackbarProps = propsFactory({
  collapsed: Object,
  loading: Boolean,
  prependAvatar: String,
  prependIcon: IconValue,
  queueGap: Number,
  queueIndex: Number,
  title: String,
  text: String,
  reverseTimer: Boolean,
  timer: {
    type: [Boolean, String],
    default: false
  },
  timerColor: String,
  timeout: {
    type: [Number, String],
    default: 5e3
  },
  vertical: Boolean,
  ...makeLocationProps({
    location: "bottom"
  }),
  ...makePositionProps(),
  ...makeRoundedProps(),
  ...makeVariantProps(),
  ...makeThemeProps(),
  ...omit(makeVOverlayProps({
    closeOnBack: false,
    transition: "v-snackbar-transition"
  }), ["persistent", "noClickAnimation", "offset", "retainFocus", "captureFocus", "disableInitialFocus", "scrim", "scrollStrategy", "stickToTarget", "viewportMargin"])
}, "VSnackbar");
var VSnackbar = genericComponent()({
  name: "VSnackbar",
  props: makeVSnackbarProps(),
  emits: {
    "update:modelValue": (v) => true
  },
  setup(props, {
    slots
  }) {
    const isActive = useProxiedModel(props, "modelValue");
    const {
      positionClasses
    } = usePosition(props);
    const {
      scopeId
    } = useScopeId();
    const {
      themeClasses
    } = provideTheme(props);
    const {
      colorClasses,
      colorStyles,
      variantClasses
    } = useVariant(props);
    const {
      roundedClasses
    } = useRounded(props);
    const countdown = useCountdown(() => Number(props.timeout));
    const overlay = ref();
    const queueItem = useSnackbarItem(isActive, () => overlay.value?.contentEl);
    let _lastOffset;
    const timerRef = ref();
    const isHovering = shallowRef(false);
    const isFocused = shallowRef(false);
    const startY = shallowRef(0);
    const mainStyles = ref();
    const hasLayout = inject(VuetifyLayoutKey, void 0);
    useToggleScope(() => !!hasLayout, () => {
      const layout = useLayout();
      watchEffect(() => {
        mainStyles.value = layout.mainStyles.value;
      });
    });
    watch(isActive, startTimeout);
    watch(() => props.timeout, startTimeout);
    onMounted(() => {
      if (isActive.value) startTimeout();
    });
    let activeTimeout = -1;
    function startTimeout() {
      countdown.reset();
      window.clearTimeout(activeTimeout);
      const timeout = Number(props.timeout);
      if (!isActive.value || timeout === -1) return;
      const element = refElement(timerRef.value);
      nextTick(() => countdown.start(element));
      activeTimeout = window.setTimeout(() => {
        isActive.value = false;
      }, timeout);
    }
    function clearTimeout() {
      countdown.reset();
      window.clearTimeout(activeTimeout);
    }
    function onPointerenter() {
      isHovering.value = true;
      clearTimeout();
    }
    function onPointerleave() {
      isHovering.value = false;
      if (!isFocused.value) startTimeout();
    }
    function onFocusin() {
      isFocused.value = true;
      clearTimeout();
    }
    function onFocusout(event) {
      const contentEl = overlay.value?.contentEl;
      if (contentEl?.contains(event.relatedTarget)) {
        return;
      }
      isFocused.value = false;
      if (!isHovering.value) startTimeout();
    }
    function onTouchstart(event) {
      startY.value = event.touches[0].clientY;
    }
    function onTouchend(event) {
      if (Math.abs(startY.value - event.changedTouches[0].clientY) > 50) {
        isActive.value = false;
      }
    }
    function onAfterLeave() {
      if (isHovering.value) onPointerleave();
      isFocused.value = false;
    }
    const locationClasses = computed(() => {
      return props.location.split(" ").reduce((acc, loc) => {
        acc[`v-snackbar--${loc}`] = true;
        return acc;
      }, {});
    });
    const queueDirection = computed(() => {
      const [side, align] = props.location.split(" ");
      return side === "bottom" || ["left", "right"].includes(side) && align === "end" ? -1 : 1;
    });
    const collapsedStyles = computed(() => {
      if (!props.collapsed) return null;
      return {
        "--v-snackbar-collapsed-height": convertToUnit(props.collapsed.height),
        "--v-snackbar-collapsed-width": convertToUnit(props.collapsed.width)
      };
    });
    const offset = computed(() => {
      if (!queueItem) return {};
      if (queueItem.offset.value === null) {
        return _lastOffset;
      }
      return _lastOffset = convertToUnit(queueItem.offset.value);
    });
    const transition = computed(() => {
      if (typeof props.transition !== "string" || !props.transition.endsWith("-auto")) {
        return props.transition;
      }
      const prefix = props.transition.replace("-auto", "");
      const [side, align] = props.location.split(" ");
      const axis = ["start", "end", "left", "right"].includes(align) || ["left", "right"].includes(side) ? "x" : "y";
      const reverse = ["end", "right"].includes(align) || !["start", "left"].includes(align) && ["bottom", "right"].includes(side) ? "-reverse" : "";
      return `${prefix}-${axis}${reverse}-transition`;
    });
    useRender(() => {
      const overlayProps = omit(VOverlay.filterProps(props), ["transition"]);
      const hasPrependMedia = !!(props.prependAvatar || props.prependIcon);
      const hasPrepend = !!(hasPrependMedia || props.loading || slots.prepend);
      const hasContent = !!(slots.default || slots.text || slots.title || props.text || props.title);
      return createVNode(VOverlay, mergeProps({
        "ref": overlay,
        "class": ["v-snackbar", {
          "v-snackbar--active": isActive.value,
          "v-snackbar--collapsed": !!props.collapsed,
          "v-snackbar--timer": !!props.timer,
          "v-snackbar--vertical": props.vertical
        }, locationClasses.value, positionClasses.value, props.class],
        "style": [mainStyles.value, {
          "--v-snackbar-offset": offset.value,
          "--v-snackbar-gap": convertToUnit(props.queueGap),
          "--v-snackbar-index": props.queueIndex,
          "--v-snackbar-direction": queueDirection.value
        }, collapsedStyles.value, props.style]
      }, overlayProps, {
        "transition": transition.value,
        "modelValue": isActive.value,
        "onUpdate:modelValue": ($event) => isActive.value = $event,
        "contentProps": mergeProps({
          class: ["v-snackbar__wrapper", themeClasses.value, colorClasses.value, roundedClasses.value, variantClasses.value],
          style: [colorStyles.value],
          onPointerenter,
          onPointerleave,
          onFocusin,
          onFocusout
        }, overlayProps.contentProps),
        "persistent": true,
        "noClickAnimation": true,
        "scrim": false,
        "scrollStrategy": "none",
        "_disableGlobalStack": true,
        "onTouchstartPassive": onTouchstart,
        "onTouchend": onTouchend,
        "onAfterLeave": onAfterLeave
      }, scopeId), {
        default: () => [genOverlays(false, "v-snackbar"), slots.header && createBaseVNode("div", {
          "class": "v-snackbar__header"
        }, [slots.header?.()]), props.timer && countdown.time.value > 0 && !isHovering.value && createBaseVNode("div", {
          "key": "timer",
          "class": normalizeClass(["v-snackbar__timer", `v-snackbar__timer--${props.timer === "bottom" ? "bottom" : "top"}`])
        }, [createVNode(VProgressLinear, {
          "ref": timerRef,
          "color": props.timerColor ?? "info",
          "max": props.timeout,
          "modelValue": props.reverseTimer ? Number(props.timeout) - countdown.time.value : countdown.time.value
        }, null)]), hasPrepend && createVNode(VDefaultsProvider, {
          "key": "prepend-defaults",
          "disabled": !hasPrependMedia && !props.loading,
          "defaults": {
            VAvatar: {
              image: props.prependAvatar
            },
            VIcon: {
              icon: props.prependIcon
            },
            VProgressCircular: {
              indeterminate: true,
              size: 24,
              width: 3
            }
          }
        }, {
          default: () => [createBaseVNode("div", {
            "class": "v-snackbar__prepend"
          }, [slots.prepend ? slots.prepend() : createBaseVNode(Fragment, null, [props.loading && createVNode(VProgressCircular, null, null), !props.loading && props.prependAvatar && createVNode(VAvatar, null, null), !props.loading && props.prependIcon && createVNode(VIcon, null, null)])])]
        }), hasContent && createBaseVNode("div", {
          "key": "content",
          "class": "v-snackbar__content",
          "role": "status",
          "aria-live": "polite"
        }, [slots.title?.() ?? (props.title ? createBaseVNode("div", {
          "class": "v-snackbar__title",
          "key": "title"
        }, [props.title]) : ""), slots.text?.() ?? props.text, slots.default?.()]), slots.actions && createVNode(VDefaultsProvider, {
          "defaults": {
            VBtn: {
              variant: "text",
              ripple: false,
              slim: true
            }
          }
        }, {
          default: () => [createBaseVNode("div", {
            "class": "v-snackbar__actions"
          }, [slots.actions({
            isActive
          })])]
        })],
        activator: slots.activator
      });
    });
    return forwardRefs({}, overlay);
  }
});
export {
  VSnackbar
};
//# sourceMappingURL=vuetify_components_VSnackbar.js.map
