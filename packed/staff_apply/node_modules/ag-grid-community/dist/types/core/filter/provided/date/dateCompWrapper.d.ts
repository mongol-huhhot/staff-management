import { IDateParams } from 'ag-grid-community/dist/types/core/interfaces/dateComponent';
import { UserComponentFactory } from 'ag-grid-community/dist/types/core/components/framework/userComponentFactory';
import { Context } from 'ag-grid-community/dist/types/core/context/context';
import { IAfterGuiAttachedParams } from 'ag-grid-community/dist/types/core/interfaces/iAfterGuiAttachedParams';
import { WithoutGridCommon } from 'ag-grid-community/dist/types/core/interfaces/iCommon';
/** Provides sync access to async component. Date component can be lazy created - this class encapsulates
 * this by keeping value locally until DateComp has loaded, then passing DateComp the value. */
export declare class DateCompWrapper {
    private dateComp;
    private tempValue;
    private disabled;
    private alive;
    private context;
    private eParent;
    constructor(context: Context, userComponentFactory: UserComponentFactory, dateComponentParams: WithoutGridCommon<IDateParams>, eParent: HTMLElement, onReady?: (comp: DateCompWrapper) => void);
    destroy(): void;
    getDate(): Date | null;
    setDate(value: Date | null): void;
    setDisabled(disabled: boolean): void;
    setDisplayed(displayed: boolean): void;
    setInputPlaceholder(placeholder: string): void;
    setInputAriaLabel(label: string): void;
    afterGuiAttached(params?: IAfterGuiAttachedParams): void;
    updateParams(params: IDateParams): void;
    private setDateCompDisabled;
}
