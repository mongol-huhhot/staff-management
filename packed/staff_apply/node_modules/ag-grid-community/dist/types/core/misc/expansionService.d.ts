import { BeanStub } from "ag-grid-community/dist/types/core/context/beanStub";
import { IExpansionService } from "ag-grid-community/dist/types/core/interfaces/iExpansionService";
import { IRowNode } from "ag-grid-community/dist/types/core/interfaces/iRowNode";
export declare class ExpansionService extends BeanStub implements IExpansionService {
    private readonly rowModel;
    private isClientSideRowModel;
    protected postConstruct(): void;
    expandRows(rowIds: string[]): void;
    getExpandedRows(): string[];
    expandAll(value: boolean): void;
    setRowNodeExpanded(rowNode: IRowNode, expanded: boolean, expandParents?: boolean, forceSync?: boolean): void;
    onGroupExpandedOrCollapsed(): void;
}
