import { RowNode } from "ag-grid-community/dist/types/core/entities/rowNode";
import { ChangedPath } from "ag-grid-community/dist/types/core/utils/changedPath";
import { RowNodeTransaction } from "ag-grid-community/dist/types/core/interfaces/rowNodeTransaction";
export interface StageExecuteParams<TData = any> {
    rowNode: RowNode<TData>;
    rowNodeTransaction?: RowNodeTransaction<TData> | null;
    rowNodeTransactions?: RowNodeTransaction<TData>[] | null;
    rowNodeOrder?: {
        [id: string]: number;
    };
    changedPath?: ChangedPath;
    afterColumnsChanged?: boolean;
}
export interface IRowNodeStage<TData = any> {
    execute(params: StageExecuteParams<TData>): any;
}
