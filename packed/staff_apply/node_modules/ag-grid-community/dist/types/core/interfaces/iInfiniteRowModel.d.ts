import { IRowModel } from "ag-grid-community/dist/types/core/interfaces/iRowModel";
import { IDatasource } from "ag-grid-community/dist/types/core/interfaces/iDatasource";
export interface IInfiniteRowModel extends IRowModel {
    setDatasource(datasource: IDatasource | undefined): void;
    refreshCache(): void;
    purgeCache(): void;
    setRowCount(rowCount: number, maxRowFound?: boolean): void;
}
