import { IAggFunc } from "ag-grid-community/dist/types/core/entities/colDef";
import { Column } from "ag-grid-community/dist/types/core/entities/column";
export interface IAggFuncService {
    addAggFuncs(aggFuncs: {
        [key: string]: IAggFunc;
    }): void;
    clear(): void;
    getDefaultAggFunc(column: Column): string | null;
    getFuncNames(column: Column): string[];
    getDefaultFuncLabel(fctName: string): string;
}
