import { BeanStub } from "ag-grid-community/dist/types/core/context/beanStub";
import { Column } from "ag-grid-community/dist/types/core/entities/column";
import { RowPosition } from "ag-grid-community/dist/types/core/entities/rowPositionUtils";
export interface CellPosition extends RowPosition {
    /** The grid column */
    column: Column;
}
export declare class CellPositionUtils extends BeanStub {
    createId(cellPosition: CellPosition): string;
    createIdFromValues(cellPosition: CellPosition): string;
    equals(cellA: CellPosition, cellB: CellPosition): boolean;
}
